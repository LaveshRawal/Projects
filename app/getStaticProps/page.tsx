import React from 'react'

const page = () => {
  return (
    <div>

      Static Generation (getStaticProps): This method is used to fetch data at build time. It allows you to pre-render the page with the fetched data, which is then served as a static HTML file. The data is shared by all the requests to that page.
      
      
      </div>
  )
}

export default page